# SafeTrace Fast Local source runtime. VLM, llama.cpp, PyInstaller, and CUDA-only
# packages are intentionally excluded. Install a CUDA-specific PyTorch build
# separately only when the target host requires it.
torch>=2.5,<2.12
torchvision>=0.20,<0.27
transformers>=4.41,<6
safetensors>=0.4,<1
faiss-cpu==1.8.0.post1
ultralytics>=8.2,<9
opencv-python>=4.10,<5
numpy>=1.26,<2
pillow>=10,<12
fastapi==0.115.6
uvicorn[standard]==0.30.6
python-multipart==0.0.9
python-dotenv>=1.0,<2
httpx==0.27.2
PyYAML==6.0.1
psutil>=5.9,<7
pytest>=8.3,<9

# Optional, disabled by default: MobileSAM refinement.
# Install only after validating the target host:
#   pip install git+https://github.com/ChaoningZhang/MobileSAM.git@master
